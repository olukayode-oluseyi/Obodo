import Signin from "@/features/auth/login/screens/Signin";
import React from "react";
import { StyleSheet } from "react-native";

const Login = () => {
  return (
    <Signin/>
  );
};

export default Login;

const styles = StyleSheet.create({});
