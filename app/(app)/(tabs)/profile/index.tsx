import ProfileScreen from '@/features/in-app/profile/screens/ProfileScreen'
import React from 'react'
import { StyleSheet } from 'react-native'

const Profile = () => {
  return (
   <ProfileScreen/>
  )
}

export default Profile

const styles = StyleSheet.create({})